{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "ic-data-repository",
  "scripts": {
    "dev": "mo-dev -d -y -g --verbose",
    "test": "mops test"
  },
  "devDependencies": {
    "mo-dev": "^0.13.0"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
