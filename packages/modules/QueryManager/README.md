# ic-data-repository-query-manager

This is the layer that handles queries. It's responsible for parsing and
executing queries.

This may just be eliminated all together or just a wrapper around CanDB.
